i = 1000000;

print(i, " times coin tossing");

tails = 0;
heads = 0;
while(i > 0)
{
    coin = dice(1, 99);

    if(coin == 0) tails = tails + 1;
    else heads = heads + 1;

    i = i - 1;
}

print("Tails ", tails);
print("Heads ", heads);
